# 易错点汇总

集中记录反复出现的错误。具体题目的细节放在对应的 `problem.md` 中。

## 类型与范围

- 根据题目数据范围选择整数类型，并检查计算过程中的中间值是否溢出。

## 表达式与标准库函数

- 不要按数学写法使用连续比较。`a == b == c` 应写成 `a == b && b == c`，`a < b < c` 应写成 `a < b && b < c`；C++ 会从左到右先得到一个布尔值，再拿这个布尔值继续比较。注意，赋值连写 `a = b = c` 是合法的。
- `std::min` 和 `std::max` 的常用形式一次只比较两个参数。比较三个数时要嵌套，例如 `max(a, max(b, c))` 和 `min(a, min(b, c))`。
- 前置自增 `++l` 会先把 `l` 加一，再产生新值；后置自增 `l++` 会先产生旧值，再把 `l` 加一。例如 `a[++l]` 使用加一后的下标，`a[l++]` 使用原下标后再移动。若它们单独写成一条语句，`++l;` 和 `l++;` 的最终效果相同。

## 循环控制

- 如果 `while` 的结束条件在循环体中被更新，而后面还会更新用于记录当前答案的位置变量，就要在条件刚刚满足时提前 `break`。例如找到第 `n` 对孪生素数后，应先执行 `if(cnt == n) break;`，再把候选位置 `i` 加 `2`；否则循环虽然会在下一次判断时结束，但 `i` 已经多更新一次，最后输出的就不是刚刚找到的答案。
- 同时要保证循环在条件尚未满足时能够继续推进。候选位置 `i += 2` 不能只写在“找到答案”的分支中，否则遇到不满足条件的候选值时，`i` 不变，程序会一直检查同一个位置并形成死循环。

## 排序

- 题目要求相等元素保持输入顺序时，考虑 `stable_sort`，或者显式记录原下标。

### 自底向上归并排序与逆序对

- 合并两个有序区间时，需要同时传入原数组 `a` 和临时数组 `tmp`：用 `a` 中的元素进行比较，把合并后的顺序写入 `tmp`，完成当前区间后再把 `tmp[l...r]` 复制回 `a`，供后续更大区间继续归并。
- 当 `a[i] > a[j]` 时，因为左半区间已经有序，`a[i]` 到 `a[mid]` 都大于右侧的 `a[j]`，所以新增逆序对数量为 `mid - i + 1`。每次合并返回本次计数，外层排序函数负责累加总数。
- 最好分成两个函数：内层函数负责一次区间合并及逆序对计数，外层函数负责枚举归并块大小和左端点，使职责清楚。
- 自底向上归并的块大小 `size` 必须从 `1` 开始并每轮乘 `2`；若从 `0` 开始，执行 `size *= 2` 后仍为 `0`，会形成死循环。
- 每次合并会处理相邻的两个长度为 `size` 的块，因此左端点按 `l += 2 * size` 移动，并且循环条件要保证右半块存在。
- 最后一组区间可能不足完整的 `2 * size` 个元素，`mid` 和 `r` 都必须限制为不超过 `n - 1`，例如使用 `min(..., n - 1)`，避免数组越界。

## 二分查找

### `lower_bound`

- `lower_bound(first, last, target)` 用二分查找返回有序区间中第一个不小于 `target` 的元素位置，即第一个满足 `value >= target` 的迭代器；需要包含 `<algorithm>`。
- 返回值是迭代器，不是元素值。使用 `*it` 取得该位置的元素，使用 `it - container.begin()` 才能取得下标。
- 如果区间中不存在不小于目标值的元素，返回值等于 `container.end()`。`end()` 指向最后一个元素之后的位置，不能解引用，因此使用 `*it` 前必须先判断 `it != container.end()`。
- 当题目要求返回“不小于查询值的最小预处理结果”时，预处理范围可能需要超过查询值的最大范围。例如查询最大为 `10^7`，下一个平衡素数可能大于 `10^7`；判断它是否平衡还需要继续得到它后面的相邻质数。

### 旋转有序数组

- 在严格递增序列形成的旋转数组中，可以将 `a[mid]` 与 `a[n - 1]` 比较：若 `a[mid] > a[n - 1]`，折点在 `mid` 右侧；否则折点在 `mid` 或其左侧。直接检查 `a[mid] < a[mid - 1]` 时，必须先保证 `mid > 0`，避免访问 `a[-1]`。
- 找到最小值下标 `minIndex` 后，可以把它视为原有序序列的起点。奇数长度的中位数下标是 `(minIndex + n / 2) % n`；偶数长度的两个中间下标是 `(minIndex + n / 2 - 1) % n` 和 `(minIndex + n / 2) % n`。
- 输出中位数要求保留小数时要转换为浮点数。偶数长度时应在加法前转换，例如 `((double)a[mid1] + (double)a[mid2]) / 2`，也能避免两个整数先相加时溢出。

### 实数二分

- 实数二分不能像整数二分一样等待左右端点相遇，需要设置精度 `EPS`，并在区间长度满足 `r - l <= EPS` 时结束。`EPS` 应比题目要求的输出精度更小，例如要求保留两位小数时可以使用 `1e-6`。
- 左端点、右端点、中点和精度都应使用 `double` 等浮点类型，例如 `double l = -100.0, r = 100.0;`。如果使用整数类型，中点的小数部分会被截断，区间也可能无法正确缩小。
- 使用实数二分前，必须先确认函数在搜索区间上具有单调性，并保证初始区间覆盖方程的根；再根据 `f(mid)` 与目标值的比较结果缩小左半区间或右半区间。

### 边界查找

- 在有序数组中寻找“最后一个满足 `start[mid] <= note` 的位置”时，不要通过 `start[mid] <= note && start[mid + 1] > note` 直接判断答案，因为 `mid` 可能等于最后一个有效下标，访问 `start[mid + 1]` 会越界。
- 可以维护变量 `answer`：每当 `start[mid] <= note` 成立，就先令 `answer = mid`，再令 `l = mid + 1` 继续向右寻找；条件不成立时令 `r = mid - 1`。循环结束后，`answer` 就是最后一个满足条件的位置。

### 查找最近位置

- 在有序数组中寻找离目标值最近的元素时，可以二分找到“最后一个 `<= target` 的位置”，然后只比较这个位置和它的下一个位置。二分中点推荐写成 `mid = l + (r - l) / 2`，避免 `l + r` 可能产生的整数溢出。
- 调用比较相邻位置的函数前，应先单独处理目标在数组两端之外的情况。右边界必须使用 `target >= values[last]`；如果漏掉等号，目标恰好等于最后一个元素时会进入函数并访问 `values[answer + 1]`，造成数组越界和未定义行为，而不是栈溢出。
- 大数组作为函数参数时使用常量引用，例如 `const vector<int>& values`，可以避免复制整个 `vector`，同时保证函数不会修改原数组。
- `vector<int> values(n)` 或 `values.resize(n)` 会真正创建 `n` 个元素，之后才能使用 `cin >> values[i]` 按下标输入；`reserve(n)` 只预留容量，不能直接访问尚未创建的元素。

## 字符串

- 输出原始表示时，不要用转换后的数值替代原字符串，否则可能丢失前导零。
- 要求“频率相同时取最早出现”时，可以先统计完整频率，再按输入顺序扫描，并且只在频率严格更大时更新答案。
- 注意区分“子串”和“子序列”：子串必须连续；子序列允许跳过字符，只要求相对顺序不变。判断子序列通常使用双指针依次匹配。
- 表示和处理普通字符串时直接使用 `string`，不要用 `vector<char>` 代替；`string` 已经提供长度、下标访问、追加和比较等字符串操作。

## 双指针

### 适用情况

- 两个序列已经有序，需要求交集、并集、差集或进行合并。比较两个指针指向的值后，可以确定移动哪一个指针，并排除已经经过的元素。
- 在一个有序序列中寻找满足条件的数对，例如两数之和，可以让指针从两端向中间移动。
- 使用前要能说明“为什么移动这个指针不会漏掉答案”。如果数据无序，而且大小关系不能帮助排除候选，就不能直接套用有序序列的双指针。

### 常见易错点

- 双指针判断两个元素相等并完成匹配后，通常要同时更新两个指针，例如 `++i; ++j;`。更一般地说，每一轮循环都必须保证至少有一个指针发生移动；如果某个分支处理完条件后两个指针都不变，下一轮仍会进入相同状态，从而形成死循环。
- 两个有序序列同步扫描时，主循环通常写成 `while (i < n && j < m)`，循环后再处理尚未结束的序列。
- 结果数量未知时，应创建空 `vector` 并使用 `push_back`；`vector<int> result(n)` 会直接创建 `n` 个元素。
- 输出可能为空时，确认题意要求是“不输出任何内容”，并避免多余空格或换行。

## 哈希表

### 适用情况

- 需要快速判断某个值是否出现过，使用 `unordered_set`。
- 需要统计每个值的出现次数，或建立“键到信息”的对应关系，使用 `unordered_map`。
- 数据没有排序，或者必须保留原顺序，不能为了使用双指针而随意排序。
- 哈希表的查找、插入平均为 `O(1)`，但需要额外空间，而且不会自动按照键的大小排序。

### 数值范围与负数

- **数值范围很大、数值稀疏或包含负数时，仍然可以使用哈希表。** 例如 `unordered_map<int, int> cnt` 可以直接使用 `-5`、`1000000000` 等整数作为键。
- 此时不能直接使用 `vector<int> cnt(maxValue + 1)` 或普通计数数组，因为范围过大会浪费内存，负数也不能直接作为数组下标。
- 因此要区分：受大范围和负数限制的是“直接下标计数数组”，不是哈希表。如果键本身可能超过 `int` 范围，应把键类型改成 `long long`。

### 常见易错点

- 建立 `unordered_map` 或 `unordered_set` 时不需要预先创建固定数量的元素，插入新键时容器会自动扩容。若提前知道大致元素数量，可以调用 `reserve` 减少扩容次数，但这只是可选的性能优化，不影响代码正确性。
- `unordered_map<int, int> cnt` 中，`cnt[a[right]]` 是以数组元素值为键查询出现次数，不是把元素值当作普通数组的物理下标。

## 滑动窗口

### 适用情况

- 题目要求处理连续子数组或连续子串，并且窗口加入、删除元素后，条件可以被增量维护时，可以使用同向双指针构成滑动窗口。
- 双指针和哈希表可以同时使用：双指针负责维护窗口边界，哈希表负责记录窗口内的元素状态。例如“最长连续不重复子数组”中，`left`、`right` 维护窗口，`unordered_map` 统计出现次数。

### 常见易错点

- 使用“和过大就移动左端点”的窗口前，先确认数组元素均为正数或非负数；存在负数时，窗口和不再具有单调性。
- 累加和优先使用 `long long`，避免以后数据范围变化时溢出。
- 维持固定长度的滑动窗口时，要区分左右端点的使用顺序：`r` 要先右移再加入新元素，因此使用 `a[++r]`；`l` 指向当前即将离开窗口的旧元素，要先删除 `a[l]`，再执行 `l++`。也就是“右端先滑动后使用，左端先使用后滑动”。如果写成 `a[++l]`，会跳过真正应该删除的旧左端元素。
- 当右指针每轮都固定向右移动一次时，优先使用 `for` 循环，让移动规则直接写在循环头中。
- 统计窗口内元素出现次数时，应先执行 `++cnt[a[right]]`，再用 `cnt[a[right]] > 1` 判断当前元素是否重复；若只判断 `!= 0` 却不加入新元素，窗口可能无法继续移动。

## 前缀和

- 必须先明确前缀和数组的下标含义。如果 `sum[i]` 表示原数组前 `i + 1` 个数之和，那么查询前 `m` 个数时要访问 `sum[m - 1]`，因为原数组下标从 `0` 开始。
- 更推荐创建长度为 `n + 1` 的数组并令 `prefix[0] = 0`，定义 `prefix[i]` 为前 `i` 个数之和。这样前 `m` 个数之和就是 `prefix[m]`，区间 `[left, right]` 的和是 `prefix[right + 1] - prefix[left]`。
- 统计序列中的“01 对”时，不必建立完整的后缀和来统计每个 `0` 右侧的 `1`。从左向右维护前缀中的 `0` 的数量 `zeroCount`，每遇到一个 `1`，就把 `zeroCount` 加入答案，更简洁且只需 `O(1)` 额外空间。两种思路本质相同：每一对可以按左侧的 `0` 统计，也可以按右侧的 `1` 统计。
- 前缀和可能比单个元素大很多，应根据数据范围考虑使用 `long long`。

### 质数区间和

- 前缀数组需要覆盖最大有效下标，因此查询值最大为 `MAXN` 时，应创建 `MAXN + 1` 个元素。这样既能访问 `prefix[MAXN]`，也能保留 `prefix[0] = 0`。
- 如果 `prefix[x]` 表示不超过 `x` 的所有质数之和，闭区间 `[L,R]` 的答案是 `prefix[R] - prefix[L-1]`。写成减去 `prefix[L]` 会在 `L` 是质数时把左端点错误地排除。
- 大量质数的累计和可能超过 `int`，累计变量和前缀和数组都应使用 `long long`。
- 按相邻质数之间的区间成段填写前缀和时，主循环无法自动处理最后一个质数到数组上界的区间，必须在循环后单独填充；若逐个位置使用 `prefix[i] = prefix[i-1] + contribution` 递推，则不需要特殊处理最后一段。

### 前缀最值

- 判断一个数是否比它左边的所有数都大时，不需要建立 `isMax` 布尔数组保存每个位置的判断结果。扫描过程中只需用一个 `int maxSoFar` 记录左侧已经出现过的最大值：若当前值大于 `maxSoFar`，就计数；随后更新 `maxSoFar`。
- `isMax` 通常让人以为它是布尔变量，这里更准确的名字是 `maxSoFar` 或 `prefixMax`。注意顺序必须是“先与左侧最大值比较，再把当前值加入最大值”，否则会拿当前数和包含自身的最大值比较。
- 建立后缀最小值数组时，先令 `suffixMin[n - 1] = a[n - 1]`，再从 `n - 2` 向左遍历，使用 `suffixMin[i + 1]` 更新当前位置。如果从 `n - 1` 开始循环，就会访问 `suffixMin[n]`，造成数组越界。
- 二维数据需要整体排序时，应使用结构体把属于同一对象的字段绑定在一起。例如奶牛路径的 `a`、`b` 应存入同一个 `Node`，再用 `vector<Node>` 排序；不能把两个字段分别排序，否则会破坏它们的对应关系。
- `vector<Node> cows(N)` 已经创建了 `N` 个元素，可以直接向 `cows[i].a` 和 `cows[i].b` 输入，不要再 `push_back`，否则容器会变成 `2N` 个元素。类似地，`vector<int> suffixMin(N)` 创建后应写 `suffixMin[N - 1] = value` 赋值，不能再声明 `int suffixMin[...]`。
- 对奶牛路径先按一侧坐标 `a` 排序，可以把二维的路径相交问题转化为另一侧坐标 `b` 上的前缀最大值和后缀最小值问题。一头奶牛安全，当且仅当它的 `b` 大于左侧所有 `b`，同时小于右侧所有 `b`。
- 如果检查安全奶牛的主循环从第二头奶牛开始，第一头奶牛没有被循环覆盖，必须额外判断。第一头奶牛左边为空，所以只需判断其 `b` 是否为全局最小值，可写成 `cows[0].b == suffixMin[0]`；不要访问 `suffixMin[1]`，否则 `N == 1` 时会越界。

## 数论

### 最大公约数

- 使用辗转相除法（欧几里得算法）时，不需要预先判断两个数谁大。即使 `a < b`，第一次计算 `a % b` 会得到 `a`，下一轮就会自然变成对 `b` 和 `a` 继续计算。
- 辗转相除法建立在递归关系 `gcd(a, b) = gcd(b, a % b)` 上，直到余数为 `0`。这种思想可以写成递归函数，也可以用 `while (b != 0)` 循环实现；“递归思想”不等于代码必须真的递归调用。

### 素数判断

- 试除法只需要检查到 `sqrt(n)`，但直接使用 `i * i <= n` 时，若 `i` 和 `n` 是 `int`，乘法可能先发生整数溢出。可以改写为 `i <= n / i`，或者写成 `1LL * i * i <= n`，先把乘法提升为 `long long`。
- 使用平方根时应先计算一次边界，例如 `int limit = sqrt(n);`，再判断 `i <= limit`，不要在每轮循环中重复调用 `sqrt`。
- 边界必须包含平方根，即使用 `<=` 而不是 `<`。例如判断 `4` 时必须检查因子 `2`；如果写成 `i < sqrt(n)` 就会漏判完全平方数。

### 质因数分解

- 可以先用埃氏筛建立质数表，再只枚举质数进行试除；筛法标记倍数时不能在第一次标记后 `break`，否则质数表中会混入没有被排除的合数。
- 对单个数 `n` 做质因数分解时，质数表只需建立到 `sqrt(n)`。枚举质因子时可用 `prime <= remaining / prime` 避免乘法溢出，并随着分解除去因子不断缩小 `remaining`。
- 只枚举到平方根时，循环结束后如果 `remaining > 1`，它本身就是最后一个质因子，指数为 `1`，必须单独加入结果。否则输入本身是质数，或分解后留下一个大质因子时会漏掉输出。

### 约数个数

- 若 `n = p1^a1 * p2^a2 * ... * pk^ak`，则正约数个数为 `(a1 + 1) * (a2 + 1) * ... * (ak + 1)`。每统计完一个质因数的指数，就可以直接把 `(指数 + 1)` 乘入答案，不需要建立哈希表。
- 筛法数组包含上界时，外层循环和标记倍数的内层循环都要包含等号。内层可写成 `j <= limit / i`，确保 `i * j` 恰好等于 `limit` 时也会被标记为合数。
- 只生成到原始 `n` 平方根的质数表时，试除结束后若 `remaining > 1`，剩余值一定是指数为 `1` 的质数，因此要执行 `answer *= 2`。这里的 `2` 只是剩余质因数对约数个数的贡献，不能写成 `answer = 2` 覆盖之前的计算结果。

### 阶乘中的质因子数量

- `n!` 中质因子 `p` 的总数量为 `floor(n/p) + floor(n/p^2) + floor(n/p^3) + ...`。循环中反复执行 `n /= p; count += n;`，就能逐层统计至少包含一个、两个、三个……因子 `p` 的数所带来的贡献。
- `a! / b!` 等于区间乘积 `(b+1) * ... * a`，所以其中质因子 `p` 的数量是 `countFactorial(a,p) - countFactorial(b,p)`。这与逐个统计区间中的每个数完全等价，但不需要遍历整个区间。
- 一个末尾零需要配对一个因子 `2` 和一个因子 `5`，因此答案是两种质因子数量的较小值。对于阶乘的商，不能直接假定因子 `2` 一定更多，例如 `5! / 4! = 5` 没有末尾零。

## 深度优先搜索

- 写 DFS 时优先确定终止条件。到达叶节点后应先保存或输出当前答案，再立即 `return`；如果只保存却不返回，代码会继续递归到合法深度之外。
- 可以先画搜索树，确定叶节点代表的完整答案，再观察每条边上的动作。回溯通常写成“加入选择 → 递归下一层 → 撤销选择”，递归调用应位于状态修改和状态恢复之间。
- `vector` 使用 `push_back(value)` 在末尾加入具体值；`pop_back()` 只删除当前最后一个元素，括号中不需要填写要删除的值。
- `vector<vector<int>>` 不能直接使用 `cout` 输出，需要用两层循环逐个访问 `result[i][j]`。外层循环更新 `i`，内层循环更新 `j`；如果内层误写 `i++`，会导致行下标越界。
- DFS 参数的含义必须前后一致。如果 `depth` 表示“还剩几个元素要选”，终止条件应为 `depth == 0`，递归时传入 `depth - 1`；如果它表示固定的目标总长度，则终止条件可用 `output.size() == depth`，递归时不能再修改 `depth`。不能混用这两套定义。
- 枚举组合时，`depth` 管“还要选择几个元素”，`start` 管“当前位置从哪个值开始选择”。选择当前值 `j` 后应调用 `DFS(j + 1, depth - 1)`；只使用 `depth` 无法知道上一层选到了哪个值，容易产生重复或顺序错误。
- 当前还需要选择 `depth` 个数时，循环上界可写成 `j <= n - depth + 1`，保证剩余范围中还有足够的元素，是组合搜索中常用的剪枝。
- 需要按照子集大小输出时，外层从 `0` 到 `n` 枚举目标大小，每个大小分别生成一棵搜索树；从 `0` 开始才能包含空集。内层再按升序枚举候选值，可保证相同大小的子集按字典序产生。
- DFS 的终止条件要与初始参数及参数含义统一。若从 `DFS(1)` 开始，并让 `idx` 表示当前准备填写的位置，则终止条件是 `idx == n + 1`；若从 `DFS(0)` 开始，并让 `idx` 表示已经填写的数量，则终止条件是 `idx == n`。
- 全排列中判断数字是否已经使用，不能写成 `for(... && exist[i] == false)`。遇到已使用数字时，这种写法会提前终止整个循环，导致后面的候选数字没有被检查。应让循环遍历全部数字，并在循环内部使用 `if(exist[i]) continue;` 跳过当前候选。
- 使用数字 `1` 到 `n` 直接作为状态数组下标时，数组或 `vector` 应开 `n + 1` 位，否则访问下标 `n` 会越界。
- 使用“选或不选”生成固定长度组合时，`k` 可以表示还需要选择的数字数量：选择当前数字后传 `k - 1`，不选择时仍传 `k`。这样每个数字只处理一次，组合中不会出现重复数字。
- 固定长度组合需要区分成功与失败两种终止状态：`k == 0` 表示已经选够，应保存答案；`idx == n + 1` 且 `k > 0` 表示数字已经用完但仍未选够，只能返回且不能输出。
- 多个终止条件可能同时成立时要注意判断顺序。组合若恰好在选择数字 `n` 后完成，下一层同时满足 `k == 0` 和 `idx == n + 1`，因此必须先判断并保存 `k == 0`，再处理 `idx == n + 1` 的失败状态。
- 每个数字允许重复选择时，可以把二叉分支写成“再选择一次当前数字”和“不再选择当前数字”。前者调用 `DFS(idx)`，让下一层仍能选择同一个数；后者调用 `DFS(idx + 1)`，转向下一种数字并且以后不再回头。
- `DFS(idx)` 的参数虽然没有变化，但只要输入全部是正数，选择当前数字后 `sum` 就会严格增大，最终由 `sum == k` 或 `sum > k` 终止，并不是完全相同状态造成的死循环。如果允许选择 `0`，则不能依赖这个条件保证终止。
- 在目标和搜索中要区分变量含义：`idx` 是数组下标，应与数组长度 `n` 比较；`k` 是目标和值。写成 `idx == k` 可能提前停止，也可能在 `idx == n` 后继续访问 `input[idx]` 导致越界。

## 动态规划

### 核心方法

- 先根据题目要求定义清楚 `dp[i]` 的含义，再寻找当前状态与前面状态 `dp[j]` 之间的递推规律。状态定义中是否要求“必须选择当前位置”“必须以当前位置结尾”等限制，会直接决定状态能否正确转移。
- `dp` 的初始值必须结合题意分析，不能习惯性初始化为 `0`。要先找出一个状态在没有其他元素参与时的合法答案，例如单个元素本身能够构成长为 `1` 的子序列，因此对应状态至少为 `1`。

### 数塔

- 先明确状态含义：`dp[i][j]` 表示从数塔第 `i` 行第 `j` 个位置出发，到达塔底能够取得的最大路径和。
- 最后一行下面已经没有其他数字，因此边界初始化为 `dp[n][i] = a[n][i]`。
- 位置 `(i,j)` 下一步只能走向左下方 `(i+1,j)` 或右下方 `(i+1,j+1)`，所以转移方程是 `dp[i][j] = max(dp[i+1][j], dp[i+1][j+1]) + a[i][j]`。
- 当前行依赖下一行已经计算好的结果，因此外层必须从下往上遍历：`for(int i = n - 1; i >= 1; i--)`。最终答案是塔顶状态 `dp[1][1]`。
- 记忆化搜索与递推写法使用相同的状态和转移关系。递归到最后一行时直接返回 `a[i][j]`；其他状态先检查 `memo[i][j]`，只在尚未计算时继续递归，避免重复搜索。

### 最大连续子序列和

- `dp[i]` 不是前 `i + 1` 个元素中的全局最大连续子序列和，而是**必须包含 `a[i]`，并且以 `a[i]` 结尾**的最大连续子序列和。这个限制保证它能够与下一个元素继续组成连续区间。
- 对于当前位置，只有“接在以 `a[i-1]` 结尾的区间后面”和“从 `a[i]` 重新开始”两种选择，因此递推式为 `dp[i] = max(dp[i-1] + a[i], a[i])`。
- 数组可能全部为负数，所以不能把 `dp[0]` 和最终答案初始化为 `0`，否则会错误地把空区间当成答案。应初始化为 `dp[0] = a[0]`、`max_dp = a[0]`。
- 这里是从 `dp[i-1]` 推出 `dp[i]` 的状态递推，不是递归函数调用。最终答案还要取所有 `dp[i]` 中的最大值。
- 如果除了最大和还要输出最优区间，可以令 `start[i]` 表示状态 `dp[i]` 对应区间的左端点。`start[i]` 是后续状态转移所需的信息，必须在每个 `dp[i]` 计算时同步更新，不能只在刷新全局最大值时赋值。
- 若 `dp[i-1] >= 0`，把 `a[i]` 接在前一区间后不会变差，应令 `start[i] = start[i-1]`；否则从当前位置重新开始，令 `start[i] = i`。特别是 `dp[i-1] == 0` 时，两种方案和相等，保留前一区间可以取得更小的左端点，所以条件应包含等号。
- `dp[i]` 的定义已经保证其对应区间的右端点就是 `i`，不需要额外建立右端点数组。按 `i` 从小到大检查最终最大和时，如果左端点相同就不更新，第一次遇到的 `i` 自然是最小右端点。

### 最长不下降子序列

- 定义 `dp[i]` 为必须选择 `a[i]`、并以 `a[i]` 结尾的最长不下降子序列长度。这样枚举 `j < i` 时，只要 `a[j] <= a[i]`，就能把 `a[i]` 接到以 `a[j]` 结尾的子序列后面。
- 每个 `a[i]` 都可以单独构成长度为 `1` 的子序列，因此每一轮都要先令 `dp[i] = 1`。不能只初始化 `dp[0]`，否则后面没有可连接前驱的位置会保留为 `0`，并继续影响其他状态。
- 状态转移为：对于所有满足 `j < i` 且 `a[j] <= a[i]` 的位置，执行 `dp[i] = max(dp[i], dp[j] + 1)`。最终答案可能在任意位置结束，所以还要取所有 `dp[i]` 的最大值。
- 题目要求“非递减”时条件使用 `<=`；只有严格递增时才使用 `<`。

### 删除元素的最大连续子序列和

- 当一个下标不足以描述所有会影响后续转移的信息时，可以增加状态维度。本题使用 `dp[i][0]` 和 `dp[i][1]`，第二维分别记录“不走删除路线”和“允许删除至多一个元素的路线”。
- `dp[i][0]` 只能从“不删除”的前一个状态继续，或者从 `a[i]` 重新开始：`dp[i][0] = max(dp[i-1][0] + a[i], a[i])`。
- 删除路线有两种来源：删除发生在前面时保留 `a[i]`，得到 `dp[i-1][1] + a[i]`；删除当前 `a[i]` 时继承 `dp[i-1][0]`。因此 `dp[i][1] = max(dp[i-1][1] + a[i], dp[i-1][0])`。
- 题目要求连续子序列，所以状态只能从相邻位置 `i-1` 转移，不能像最长不下降子序列那样枚举任意 `j < i`，否则会无代价地跳过多个元素。
- 第一个位置必须单独初始化，随后从 `i = 1` 开始递推，否则会访问 `dp[-1]`。当前写法令 `dp[0][0] = dp[0][1] = a[0]`，既保留非空序列，也符合“可以删除 0 个或 1 个元素”。
- 连续子序列不能为空，元素又可能全部为负数，因此不能把初始状态或最终答案设为 `0`；应从 `a[0]` 开始，否则会错误地把空序列当作答案。

### 最长公共子序列

- 定义 `dp[i][j]` 为 `s1` 前 `i` 个字符与 `s2` 前 `j` 个字符的最长公共子序列长度。`i`、`j` 分别代表两个字符串的前缀长度，必须用两层循环枚举全部组合，不能让二者绑定着一起增加。
- `dp` 下标表示字符数量，因此循环范围为 `1...length`，必须包含等号；对应的字符串末尾字符下标是 `i-1` 和 `j-1`。如果循环写成 `< length`，最终输出的 `dp[length1][length2]` 根本没有被计算。
- 两个末尾字符相等时可以配对：`dp[i][j] = dp[i-1][j-1] + 1`。不相等时它们不能互相配对，至少有一个不参与答案，因此取 `max(dp[i-1][j], dp[i][j-1])`。
- 左边 `dp[i][j-1]` 表示不考虑 `s2` 当前末尾字符，上边 `dp[i-1][j]` 表示不考虑 `s1` 当前末尾字符。保留下来的另一个字符只是仍然可选，并不是必须选择，所以“两个末尾字符都不选”的情况也已经包含在这两个状态中。
- 第 `0` 行或第 `0` 列代表其中一个字符串为空，最长公共子序列长度为 `0`。全局数组会自动初始化为 `0`；局部数组应显式写成 `{0}`。

### 最长回文子串

- 定义 `dp[i][j]` 表示连续子串 `s[i...j]` 是否为回文串。长度至少为 `3` 时，只有两端字符相等且内部子串也是回文串，整个区间才是回文串：`s[i] == s[j] && dp[i+1][j-1]`。
- 长区间依赖长度少 `2` 的内部区间，因此应先初始化长度为 `1` 和 `2` 的区间，再按照 `len = 3, 4, ...` 的顺序递推。
- 单个字符一定是回文串，因此 `dp[i][i] = true`；长度为 `2` 的区间没有可供正常转移的内部区间，需要单独判断相邻字符是否相等。
- 局部 `bool` 数组不会自动初始化。只给部分位置赋值时，其他位置可能是随机值，所以应写成 `bool dp[MAXN][MAXN] = {false};`。
- 判断 `s[i] == s[i+1]` 时必须保证 `i + 1 < s.size()`。长度为 `2` 的初始化循环应写到 `i < s.size() - 1`，否则最后一次会访问越界。

### 背包问题

- 定义 `dp[i][j]` 表示只考虑前 `i` 件物品、背包允许的总重量不超过 `j` 时能够取得的最大价值。第一维记录物品范围，第二维记录容量上限。
- 对每件物品都必须计算容量 `0...v` 的所有状态。后面的转移可能访问任意剩余容量 `j-w[i]`，所以不能只计算最终容量 `v`；容量 `v` 本身也是合法状态，循环应写成 `j <= v`。
- 当 `w[i] > j` 时当前物品装不下，只能继承 `dp[i-1][j]`；能够装下时，在不选的 `dp[i-1][j]` 和选择后的 `dp[i-1][j-w[i]] + c[i]` 之间取最大值。
- 选择当前物品时仍从第 `i-1` 行转移，保证每件物品最多选择一次。如果从当前第 `i` 行转移，就可能重复选择同一件物品。
- 数组下标规则必须统一。若 `w`、`c` 从 `1` 开始输入，DP 循环也可以直接使用 `w[i]`、`c[i]`；若物品数组从 `0` 开始，而 `dp` 的物品数量从 `1` 开始，则当前物品必须写成 `w[i-1]`、`c[i-1]`。
- 01 背包与完全背包的关键区别在“选择当前物品”后的来源：01 背包使用 `dp[i-1][j-w[i]] + c[i]`，回到上一行以避免重复选择；完全背包使用 `dp[i][j-w[i]] + c[i]`，留在当前行以允许继续选择同一种物品。
- 二维完全背包可以把容量放在外层并从小到大枚举、物品放在内层。此时 `dp[i-1][j]` 已在当前容量的上一轮物品中算好，`dp[i][j-w[i]]` 也已在更小容量时算好。

### 楼梯方案计数

- 定义 `dp[i]` 为从第 `0` 号台阶到达第 `i` 号台阶的方案数。起点本身是一种合法初始状态，因此必须设置 `dp[0] = 1`；如果从 `dp[1] = 1` 开始，就会错误地要求所有路线都先经过第 `1` 号台阶，漏掉从起点直接跨过去的方案。
- 最后一步可以跳 `1...k` 级，因此 `dp[i]` 要累加所有合法的 `dp[i-j]`。`j == i` 表示从第 `0` 号台阶直接跳到第 `i` 号台阶，是合法情况；只有 `j > i` 时才会越过起点并停止循环。
- `dp[i]` 在内层循环中不断与自身相加，所以除起点 `dp[0] = 1` 外，其余位置必须先初始化为 `0`。遇到有水的台阶时令 `dp[i] = 0`，表示不能落在该位置。
- 方案数可能在递推过程中就超过整数范围，不能等到输出时才取余。每次累加都应执行 `dp[i] = (dp[i] + dp[i-j]) % MOD`。
